This project is forked from ibus-tmpl.

A real traditional Chinese zhuyin input method for GNU/Linux.

$ autoreconf -if
$ ./configure --prefix=/usr
$ make
$ sudo make install
$ ibus-daemon -r -d -x

P.S. It doesn't work yet.
