This project is forked from ibus-tmpl.

ibus-zhuyin - A zhuyin (phonetic) Chinese input method for GNU/Linux.

Licensed by GPLv3

$ autoreconf -if
$ ./configure --prefix=/usr
$ make
$ sudo make install
$ ibus-daemon -r -d -x

P.S. It doesn't work yet.
