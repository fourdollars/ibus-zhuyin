This project is forked from ibus-tmpl.

A real traditional zhuyin Chinese input method.

$ ./autogen.sh
$ ./configure --prefix=/usr
$ make
$ sudo make install
$ ibus-daemon -r -d -x

P.S. It doesn't work yet.
